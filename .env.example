# Copy this file to .env and/or export these variables in your terminal.
HF_TOKEN=your_huggingface_token_here
HF_MODEL=Qwen/Qwen2.5-7B-Instruct

# Optional: Kaggle credentials are configured separately using Kaggle's
# supported authentication/API-key flow.
